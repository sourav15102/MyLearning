### Integers:
- Integer.MAX_VALUE

### Map:
- mp.getOrDefault(c,0) // 0 is the default value
- mp.containsKey(key)

### Arrays:
- Arrays.fill(arr,0);
- Arrays.sort(new int[])
- Arrays.sort(int[], Collections.reverseOrder()).
- 